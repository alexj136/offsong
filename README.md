# Offsong

A vibe-coded ChordPro renderer that runs in a web browser.

## Setup

On Ubuntu:
```
sudo apt install python3-flask python3-watchdog
python3 app.py --dir /path/to/song/files # (defaults to current dir)
```
Then open `http://localhost:5000`.
